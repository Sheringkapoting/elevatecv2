
import { Link } from "react-router-dom";

const Logo = () => (
  <Link to="/" className="flex items-center text-primary-600 font-bold text-xl">
    {/* You may replace this with an actual logo or SVG if desired */}
    Enhance CV
  </Link>
);

export default Logo;
